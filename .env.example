# Cloud Run Deployment Configuration
# Copy this file to .env and update with your values
# The deploy.sh script will use these variables if set

# GCP Project ID (required)
# If not set, will use the current gcloud project
GCLOUD_PROJECT=xinjiangshao

# Cloud Storage Bucket (required)
# This is where your images are stored
CLOUD_STORAGE_BUCKET=xinjiangshao.appspot.com

# Cloud Run Region (optional)
# Default: us-central1
REGION=us-central1

# CORS Origin Allow List (optional)
# Comma-separated list of allowed origins, or * for all
# Default: *
CORS_ORIGIN_ALLOW_LIST=*

# Service Account (optional)
# If not set, uses the default compute service account
# SERVICE_ACCOUNT=ext-image-processing-api@xinjiangshao.iam.gserviceaccount.com

# Allow Unauthenticated Access (optional)
# Set to "true" to allow public access, "false" to require authentication
# Default: false (will prompt during deployment if not set)
ALLOW_UNAUTHENTICATED=true
